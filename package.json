{
  "name": "my-vercel-proxy",
  "version": "1.0.0",
  "description": "A simple proxy on Vercel to handle CORS",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}
